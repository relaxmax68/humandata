{# bootstrap_3_layout.html.twig #}
{% form_theme form 'TestsBundle:form:fields.html.twig' %}
{% extends "::bootstrap.html.twig" %}


{% block stylesheets %}
    {{ parent() }}
      <link rel="stylesheet" href= "{{ asset('/css/tests.css') }}" />
{% endblock %}

{% block title %}Menu{% endblock %}

{% block body %}

    <h3>Menu</h3>
{#}
    {<div>
	    {{ form_label(form.age) }}
	    {{ form_errors(form.age) }}
	    {{ form_widget(form.age) }}
	</div>
{#}

	<div class="row">
	  <div class="col-lg-6">
	    <div class="input-group">
	      <span class="input-group-btn">
	        <button type="button" class="btn btn-default">Action</button>
	      </span>
	      <input type="text" class="form-control">
	    </div><!-- /input-group -->
	  </div><!-- /.col-lg-6 -->
	</div><!-- /.row -->
    <input type="submit" value="Create" class="btn btn-default pull-right" />

    {{ form_start(form) }}

	{{ form_end(form) }}




	<div class="row">
		TESTS:
		{% for test in tests %}
		    {{ test.name }}
		{% endfor %}
	</div>

	<a class="btn btn-primary btn-xs" role="button" href="{{path('accueil_homepage')}}">Retour à l'accueil</a>
	
{% endblock %}



